name: Build and publish Android APK

on:
  workflow_dispatch:
    inputs:
      version:
        description: Release version, including the v prefix
        required: true
        default: v0.2.0
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: android-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Test, sign, and publish
    runs-on: ubuntu-latest
    env:
      RELEASE_VERSION: ${{ github.event_name == 'workflow_dispatch' && inputs.version || github.ref_name }}

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up JDK 11
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "11"
          cache: gradle

      - name: Validate release configuration
        env:
          ANDROID_SIGNING_KEY: ${{ secrets.ANDROID_SIGNING_KEY }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          [[ "$RELEASE_VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]] || {
            echo "Release version must use the vMAJOR.MINOR.PATCH format" >&2
            exit 1
          }
          test -n "$ANDROID_SIGNING_KEY" || { echo "ANDROID_SIGNING_KEY is not configured" >&2; exit 1; }
          test -n "$ANDROID_KEYSTORE_PASSWORD" || { echo "ANDROID_KEYSTORE_PASSWORD is not configured" >&2; exit 1; }

      - name: Restore release signing key
        env:
          ANDROID_SIGNING_KEY: ${{ secrets.ANDROID_SIGNING_KEY }}
        run: |
          printf '%s' "$ANDROID_SIGNING_KEY" | base64 --decode > "$RUNNER_TEMP/personal-ai-os-release.keystore"
          chmod 600 "$RUNNER_TEMP/personal-ai-os-release.keystore"

      - name: Test and assemble signed release
        env:
          ANDROID_KEYSTORE_PATH: ${{ runner.temp }}/personal-ai-os-release.keystore
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: personal-ai-os
        run: ./gradlew --no-daemon testDebugUnitTest assembleRelease

      - name: Verify and package APK
        run: |
          APK_SOURCE="app/build/outputs/apk/release/app-release.apk"
          APK_DESTINATION="dist/Personal-AI-OS-${RELEASE_VERSION}.apk"
          test -f "$APK_SOURCE" || { echo "Signed release APK was not generated" >&2; exit 1; }
          mkdir -p dist
          cp "$APK_SOURCE" "$APK_DESTINATION"
          APKSIGNER="$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -1)"
          test -n "$APKSIGNER" || { echo "apksigner was not found" >&2; exit 1; }
          "$APKSIGNER" verify --verbose "$APK_DESTINATION"
          sha256sum "$APK_DESTINATION" > "$APK_DESTINATION.sha256"

      - name: Create or update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          APK="dist/Personal-AI-OS-${RELEASE_VERSION}.apk"
          CHECKSUM="$APK.sha256"
          if gh release view "$RELEASE_VERSION" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$RELEASE_VERSION" "$APK" "$CHECKSUM" --clobber --repo "$GITHUB_REPOSITORY"
          else
            gh release create "$RELEASE_VERSION" "$APK" "$CHECKSUM" \
              --repo "$GITHUB_REPOSITORY" \
              --target "$GITHUB_SHA" \
              --title "Personal AI OS ${RELEASE_VERSION}" \
              --generate-notes \
              --latest
          fi
