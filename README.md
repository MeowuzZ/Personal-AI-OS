# 知行 · Personal AI OS Android

根据 `docs/prd/01-personal-ai-os.md` 实现的离线优先安卓应用。它面向个人日常使用，将任务、日程、笔记、长期目标、个人信息、可信助手和每日复盘放在同一套本地数据中。

当前版本：**v0.2.0**

## 下载与安装

前往 [GitHub Releases](https://github.com/MeowuzZ/Personal-AI-OS/releases/latest)，在 **Assets** 中下载与版本号对应的 APK，例如：

```text
Personal-AI-OS-v0.2.0.apk
```

下载后在安卓手机上打开 APK；首次安装时需要允许浏览器或文件管理器“安装未知应用”。Release 同时提供 `.sha256` 文件用于校验下载完整性。

## 更新日志

### v0.2.0 · 2026-08-05

- 新增个人信息模块，支持姓名、性别符号、生日和醒目座右铭编辑，并整合数据管理与 AI 权限设置。
- 新增生日当天上午 8:00 本地通知，支持设备重启后重新调度。
- 删除 Inbox；首页仅显示今日日程与待办，将每日复盘并入 AI 助手。
- 完善任务表单，支持标题、日期、时间、最长 24 小时持续时间、内容和自定义标签。
- 支持展开任务详情、编辑、删除和删除二次确认。
- 新增任务列表、日期时间条和标签分类三种视图。
- 新增绿色“进行中”和红色“逾期”状态；应用启动时弹窗提示逾期任务，可直接完成任务。
- 全局悬浮记录按钮精简为“+”，底部导航改为纯文字。
- 重构长期目标系统，支持主任务与子任务独立进度、自动完成和取消主任务后的子任务进度恢复。
- 新增 AI 每日复盘、超过 8 小时的休息鼓励、23:00—次日 6:00 首页入口和日历历史回看。
- 增加旧版数据迁移、11 项单元测试及 Android 36 模拟器验收。
- 新增固定签名的 GitHub Actions 自动构建流程，版本发布后可从 GitHub Releases 直接下载 APK 和 SHA-256 校验文件。

### v0.1.0 · 2026-08-05

- 根据 Personal AI OS PRD 建立 Android MVP。
- 实现 Today、任务、日历、目标、知识库、助手和本地数据管理基础能力。

> 维护约定：后续每次推送功能或行为变更时，必须同步更新应用版本号，并在本节顶部记录日期、版本号和更新内容；代码、版本与日志应位于同一提交。

## 已实现

- Today：仅展示当日待办与日程；23:00—次日 6:00 显示每日复盘入口。
- 快速记录：全局“+”按钮离线识别任务、笔记和日程，确认后才写入。
- 任务：标题、日期、开始时间、最长 24 小时持续时间、具体内容与标签；支持详情展开、编辑和二次确认删除。
- 任务状态：根据本地时间自动显示待开始、绿色进行中、红色逾期和已完成；每次启动弹窗提醒逾期任务。
- 任务视图：列表、14 天时间条与自定义标签分类视图。
- 目标：独立于日程的长期目标系统，主任务和子任务均可标记；主任务取消完成会恢复子任务原进度。
- 知识库：笔记、标签、目标关联和本地关键词搜索。
- Assistant：本地检索任务/笔记/目标，回答附带可打开的来源片段；可生成任务草稿。
- 每日复盘：在助手中生成当天完成清单；安排超过 8 小时会附加休息提醒，保存后可从日历回看。
- 个人信息：姓名、性别符号、生日、醒目座右铭、AI 权限、执行记录和数据管理。
- 生日提醒：读取设备本地时间，在生日当天上午 8:00 发送本地通知。
- 隐私开关、JSON 全量导出、执行日志、撤销和二次确认清空。
- 首次启动可选择空白空间或隔离的演示数据。

应用不声明网络权限，当前数据全部保存在 Android `SharedPreferences` 中。真正的云端大模型、邮箱账号和外部日历同步需要安全后端，未在 APK 中硬编码密钥。

## 开发环境

- JDK 11
- Android SDK 33
- Kotlin 1.8.0
- Jetpack Compose 1.4.3 / Material 3
- Gradle Wrapper 8.5

## 项目结构

```text
.
├── app/                         # Android 应用源码与测试
│   └── src/
│       ├── main/                # Kotlin、Compose UI、资源与 Manifest
│       └── test/                # 单元测试
├── docs/prd/                    # 产品需求文档
├── gradle/wrapper/              # 可复现的 Gradle Wrapper
├── AGENTS.md                    # 仓库维护与发版规则
└── README.md
```

## 构建与测试

Android Studio 打开本目录后，等待同步完成即可运行 `app`。命令行：

```bash
export ANDROID_HOME="$HOME/Library/Android/sdk"
./gradlew testDebugUnitTest assembleDebug
```

调试 APK 输出到：

```text
app/build/outputs/apk/debug/app-debug.apk
```

仓库维护者可在 GitHub 的 **Actions → Build and publish Android APK → Run workflow** 中输入版本号，自动完成测试、签名、构建和 Release 上传。正式签名通过 GitHub Secrets 注入，不存放在仓库中。

安装到已连接的安卓手机（需开启 USB 调试）：

```bash
$ANDROID_HOME/platform-tools/adb install -r app/build/outputs/apk/debug/app-debug.apk
```

## 日常使用建议

1. 首次启动选择“从空白空间开始”；想快速了解功能可先加载演示数据。
2. 点击首页头像填写姓名、性别、生日与座右铭，并允许通知和精确闹钟权限。
3. 在任务页用完整表单记录日期、时间、持续时间、内容和标签。
4. 任何页面点击右下角“+”，可快速输入自然语言并检查解析预览。
5. 把稳定经验写入知识库，随后可在助手中用自然语言检索。
6. 每晚在助手中编辑每日复盘，保存后可从对应日期的日历回看。
7. 定期在个人信息页导出 JSON 备份。

## 当前边界

- 本地助手是可解释的关键词/规则引擎，不会假装成云端大模型。
- 除生日提醒和应用内逾期弹窗外，暂无普通任务系统通知、邮箱登录、多设备同步、外部日历同步和语义向量检索。
- 数据量较大或需要跨设备时，下一阶段应迁移到 Room 数据库，并新增带用户隔离的后端服务。
